import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // If you have global styles, ensure this file exists
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);

